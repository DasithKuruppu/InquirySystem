#include "Navigator.h"
#include "Inputs.h"
using namespace std;
void main()
{
	//initializeDefinitions();
	
	//printDefinitions(getDefinitions("User"));
	
	
		//update(originalrecord,updaterecord, loc);
		//printRecord();

		LoginMenu();
	system("Pause");
}